<a 
    class={`
        bg-white/5 
        border border-black/10 rounded-full 
        flex justify-center items-center gap-x-2 
        py-1 px-2 md:py-2 md:px-4 
        text-xs lg:text-base
        hover:scale-110 hover:bg-black/10 
        transition
        min-w-[100px] min-h-[40px] // Tamaño mínimo para dispositivos móviles
        max-w-[200px] max-h-[60px] // Tamaño máximo para dispositivos móviles
        md:min-w-[150px] md:min-h-[50px] // Tamaño mínimo para dispositivos de escritorio
        md:max-w-[250px] md:max-h-[70px] // Tamaño máximo para dispositivos de escritorio
    `}
    {...Astro.props}
    target="_blank"
    rel="noopener noreferrer"
>
    <slot />
</a>

